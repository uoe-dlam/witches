#!/usr/bin/env groovy

pipeline {
    agent any

    options {
        ansiColor('xterm')
    }

    stages {
        stage('Test') {
           steps {
               echo 'I am in the witches site'
               echo 'I am being triggered by Github'
           }
        }
    }
}
